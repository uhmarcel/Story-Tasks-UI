import { ColorStylePipe } from './color-style.pipe';

describe('ColorStylePipe', () => {
  it('create an instance', () => {
    const pipe = new ColorStylePipe();
    expect(pipe).toBeTruthy();
  });
});
