import { NormalizeCasePipe } from './normalize-case.pipe';

describe('NormalizeCasePipe', () => {
  it('create an instance', () => {
    const pipe = new NormalizeCasePipe();
    expect(pipe).toBeTruthy();
  });
});
